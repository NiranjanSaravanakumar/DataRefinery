# ==============================================================================
# DataRefinery — Python Dependencies
# ==============================================================================
# Python version requirement: >=3.11
# ==============================================================================

# ------------------------------------------------------------------------------
# Runtime Dependencies — Flask Web Application
# Install with: pip install -r requirements.txt
# ------------------------------------------------------------------------------

# Web framework — powers the DataRefinery web interface
flask>=3.0.0

# Environment variable loader — reads .env into os.environ
python-dotenv>=1.0.0

# HTTP utilities — file validation, secure filenames (bundled with Flask)
Werkzeug>=3.0.0

# ------------------------------------------------------------------------------
# Development & Testing Dependencies
# ------------------------------------------------------------------------------

# Test runner — used to discover and execute tests in the tests/ directory
pytest>=8.0.0

# Code formatter — enforces PEP 8 style automatically
black>=24.0.0

# Linter — static analysis for bugs, style, and complexity
ruff>=0.4.0

# Type checker — validates type annotations across the codebase
mypy>=1.10.0
